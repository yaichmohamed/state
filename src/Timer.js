import React from 'react';

const Timer = ({ timeInterval }) => {
  return <p>Component mounted for {timeInterval} seconds.</p>; // Display the timeInterval value in seconds
};

export default Timer;
